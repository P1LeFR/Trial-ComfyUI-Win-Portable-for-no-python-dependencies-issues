# pak7.txt — utilitaires complémentaires (sans versions figées)
# Politique : rester souple; éviter les doublons; limiter les pins aux profiles/constraints du workflow.

aenum
black                    # outil dev/formatage (ok en runtime, mais optionnel)
blend_modes              # (= blend-modes / blend_modes ; nom PyPI courant: blend_modes)
facexlib
fvcore
gguf
gpytoolbox
hydra-core               # ⚠ dépend d'omegaconf (déjà en pak4) → tester ensemble
iopath
loguru
peft
protobuf                 # ⚠ peut entrer en conflit si d'autres libs pin (ONNX/TF). Laisser non figé ici.
pydub                    # nécessite ffmpeg présent dans PATH (déjà ajouté en stage1)
pymatting
requests
tomli; python_version < "3.11"   # backport seulement pour anciens Python

# (Doublon évité ici)
# git+https://github.com/cozy-comfyui/cozy_comfyui.git   # ← déjà listé dans pak6

# Notes :
# - Aucun pin ici pour rester non figé. Si une régression survient (ex. protobuf/hydra),
#   gère un cap temporaire via un constraints.txt injecté par le workflow.
# - hydra-core + omegaconf : garder l’ordre d’installation (pak4 avant pak7) limite les conflits.
# - pydub : ton stage1 ajoute déjà ffmpeg.exe dans python_standalone/Scripts, donc OK.
