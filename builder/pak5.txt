# pak5.txt — utilitaires et libs complémentaires (sans versions figées)
# Politique : rester souple ; n’utiliser des pins que comme FILET optionnel.

# --- Réseaux / E/S / outils ---
aiohttp>=3.9.5
yarl                     # ⚠ Par défaut, pas de pin. Voir plus bas le filet optionnel Windows.
cachetools
filelock
psutil
pyyaml
python-dateutil
requirements-parser
rich
rich-argparse
tqdm
typer
uv
yapf

# Backport stdlib uniquement si nécessaire (inutile en Python 3.12+)
importlib-metadata; python_version < "3.10"

# --- Vision / média / audio ---
av
imageio-ffmpeg
mss
pilgram
qrcode[pil]
soundfile
trimesh[easy]
webcolors

# --- Modèles / NLP / vision IA ---
clip-interrogator
colour-science
einops
segment-anything
sentencepiece
safetensors
tokenizers
ultralytics

# --- Traitement d’images / data science ---
albumentations
deepdiff
dill
numba
numexpr
pandas
simpleeval
spandrel
SVGLib
toml
typing-extensions

# --- Git / GitHub / NV / DB / ORM ---
GitPython
PyGithub
pygit2
pynvml
SQLAlchemy
alembic
matrix-nio
yarl

# --- Modules PyTorch complémentaires (laissent Torch géré par pak3) ---
torchsde

# ------------------------------------------------------------------------------
# ⚙️ Notes & garde-fous (OPTIONNELS) :
#
# 1) YARL sous Windows : si une future release casse aiohttp sur Win,
#    tu peux TEMPORAIREMENT pinner (dans un profil dédié) :
#    # yarl==1.9.4 ; sys_platform == "win32"
#    Préfère gérer cela via un constraints.txt injecté par le workflow.
#
# 2) importlib-metadata :
#    gardé avec marqueur (seulement pour vieux Pythons). En 3.12+, inutile.
#
# 3) ultralytics :
#    peut tirer opencv-python ; tu as déjà opencv-contrib-python-headless en pak4.
#    En cas de conflit, gère la préférence via l’ordre d’installation (pak4 avant pak5)
#    ou un constraints.txt (excluant opencv non-headless).
#
# 4) Torch écosystème :
#    ne pas mettre xformers ici (géré à part après pak3). torchsde ne réinstalle pas torch.
#
# 5) Aucune version figée ici : pour une build 100% reproductible ponctuelle,
#    utilise un constraints.txt externe au dépôt (injecté par le workflow).
# ------------------------------------------------------------------------------
