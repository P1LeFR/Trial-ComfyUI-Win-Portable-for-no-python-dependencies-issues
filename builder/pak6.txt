# pak6.txt — extras GPU / nodes (CUDA requis, ORT-GPU activé)

# ── ONNX Runtime GPU (CUDA) ───────────────────────────────────────────────────
onnxruntime-gpu
nvidia-ml-py            # supprime l’avertissement pynvml manquant

# ── CUDA utils ────────────────────────────────────────────────────────────────
cupy-cuda12x            # CUDA 12.x (cu128/cu129)

# ── Dépendances “node-driven” (générales) ─────────────────────────────────────
pixeloe
scikit-build-core
compel
lark
torchdiffeq
fairscale
attrdict

# ── Vision/face ───────────────────────────────────────────────────────────────
insightface             # privilégier PyPI (GPU activé si dispo)
# dlib-bin              # Windows wheels via PyPI (fallback)
# dlib                  # autres plateformes (peut nécessiter build)

# ✳ Si une wheel précise est indispensable pour ta combo (Windows cp312/torch):
# https://github.com/eddiehe99/dlib-whl/releases/download/v20.0.0-alpha/dlib-20.0.0-cp312-cp312-win_amd64.whl
# https://github.com/Gourieff/Assets/raw/refs/heads/main/Insightface/insightface-0.7.3-cp312-cp312-win_amd64.whl

# ── Nunchaku (couplé à Torch/Python) ──────────────────────────────────────────
# Décommente SEULEMENT la wheel qui correspond exactement à ta série Torch & cp
# https://github.com/nunchaku-tech/nunchaku/releases/download/v1.0.0/nunchaku-1.0.0+torch2.8-cp312-cp312-win_amd64.whl
# (si tu es en Torch 2.7, prends la wheel correspondante; sinon garde PyPI vide)

# ── Cozy utils ────────────────────────────────────────────────────────────────
git+https://github.com/cozy-comfyui/cozy_comfyui.git#egg=cozy-comfyui

# ── Dépendances Git (sources officielles) ─────────────────────────────────────
git+https://github.com/rodjjo/filterpy.git
git+https://github.com/openai/CLIP.git
git+https://github.com/ltdrdata/cstr.git
git+https://github.com/ltdrdata/ffmpy.git
git+https://github.com/ltdrdata/img2texture.git
git+https://github.com/facebookresearch/sam2
# Janus (deps dans son pyproject)
git+https://github.com/deepseek-ai/Janus.git

# Notes:
# - Ne mélange pas onnxruntime (CPU) et onnxruntime-gpu dans tes paquets.
# - cupy-cuda12x couvre CUDA 12.x (cu128/cu129). Ajuste si tu changes de canal CUDA.
# - Les wheels Nunchaku/Dlib/InsightFace fixes sont à activer seulement si nécessaire.
